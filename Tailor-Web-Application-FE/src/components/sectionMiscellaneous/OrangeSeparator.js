/* eslint-disable arrow-body-style */
import { Box } from '@mui/material';

const OrangeSeparator = ({ h, w }) => {
    return (
        <Box sx = {{ 
            height: '100%', 
            width: '6px', 
            backgroundColor: '#DEB18A',
            color: '#DEB18A'}}>
            I
        </Box>
    );
};

export default OrangeSeparator;